# 🚀 Инструкция по установке и запуску Suntier

## Шаг 1: Установка зависимостей

```bash
cd suntier
npm install
```

Это установит:
- `express` - API сервер
- `cors` - CORS middleware
- `concurrently` - Запуск нескольких команд одновременно
- И все остальные зависимости

## Шаг 2: Запуск проекта

### Вариант A: Запуск всего сразу (рекомендуется)

```bash
npm run dev:all
```

Эта команда запустит:
1. **Vite dev server** на http://localhost:5173 (фронтенд)
2. **API server** на http://localhost:3001 (бэкенд)

### Вариант B: Запуск по отдельности

**Терминал 1 - Фронтенд:**
```bash
npm run dev
```

**Терминал 2 - API сервер:**
```bash
npm run server
```

## Шаг 3: Открыть приложение

Откройте браузер и перейдите на:
```
http://localhost:5173
```

## Шаг 4: Войти в админку

1. Нажмите кнопку "Войти" в правом верхнем углу
2. Введите имя пользователя: `admin`
3. Нажмите "Войти"
4. В хедере появится кнопка "⚙️ Админка"
5. Нажмите на неё для перехода в админ-панель

## Проверка работы

### Проверка фронтенда
- Откройте http://localhost:5173
- Должна загрузиться главная страница
- Если игроков нет, увидите сообщение "Нет игроков в рейтинге"

### Проверка API сервера
- Откройте http://localhost:3001/api/players
- Должен вернуться JSON с игроками (или пустой массив `[]`)

## Возможные проблемы

### Порт 5173 занят
```bash
# Vite автоматически выберет другой порт (5174, 5175 и т.д.)
# Проверьте вывод в терминале
```

### Порт 3001 занят
Измените порт в `server.js`:
```javascript
const PORT = 3002; // Измените на свободный порт
```

И в `src/components/Admin.tsx` и `src/App.tsx`:
```typescript
fetch('http://localhost:3002/api/players')
```

### Ошибка "Cannot find module"
```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
```

### Ошибка CORS
Убедитесь, что в `server.js` подключен `cors`:
```javascript
import cors from 'cors';
app.use(cors());
```

## Сборка для продакшена

```bash
npm run build
```

Результат будет в папке `dist/`

## Предпросмотр продакшен сборки

```bash
npm run preview
```

## Структура портов

| Сервис | Порт | URL |
|--------|------|-----|
| Фронтенд (Vite) | 5173 | http://localhost:5173 |
| API сервер | 3001 | http://localhost:3001 |
| Preview | 4173 | http://localhost:4173 |

## Следующие шаги

1. ✅ Установили зависимости
2. ✅ Запустили проект
3. ✅ Открыли в браузере
4. ✅ Вошли в админку

Теперь можете:
- Добавлять игроков
- Редактировать режимы и тиры
- Экспортировать данные

Подробнее: [ADMIN_GUIDE.md](./ADMIN_GUIDE.md)

---

Если возникли проблемы, проверьте:
1. Node.js версии 18 или выше: `node --version`
2. npm установлен: `npm --version`
3. Все зависимости установлены: `npm install`
4. Оба сервера запущены: `npm run dev:all`
